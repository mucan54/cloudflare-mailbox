{
  "name": "cloudflare-mailbox-inbound-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Email Worker that forwards inbound mail to the Laravel webhook",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "postal-mime": "^2.2.0"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
